<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Constantes magicas en PHP</title>
</head>

<body>
    <?php
    //Estas son las constantes magicas en PHP
    /**
     * __CLASS__ => Devuelve el nombre de la clase, si se utiliza adentro
     * __DIR__ => Directorio del archivo
     * __FILE__ => Nombre del archivo, incluida la ruta completa
     * __FUNCTION__ => Si esta dentro de una funcion, devuelve el nombre de la funcion
     * __LINE__ => Numero de linea actual
     * __METHOD__ => Si se usa dentro de una función que pertenece a una clase, se devuelve tanto el nombre de la clase como el de la función.
     * __NAMESPACE__ => Si se usa dentro de un espacio de nombres, se devuelve el nombre del espacio de nombres.
     * __TRAIT__ => Si se usa dentro de un rasgo, se devuelve el nombre del rasgo.
     * ClassName::class => Devuelve el nombre de la clase especificada y el nombre del espacio de nombres, si lo hay.
     */
    ?>
</body>

</html>